/**
 * A RationalNumber can be represented by a string.
 */
export type RationalNumber = string;

/**
 * Check that given argument is a `RationalNumber`.
 */
export declare const isRationalNumber: (arg: unknown) => arg is RationalNumber;
